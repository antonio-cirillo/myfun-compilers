fun fibonacci(integer number): integer
    if number = 0 or number = 1 then
        return number;
    else
        return number + fibonacci(number - 1);
    end if;
end fun;

main

    integer number;
    integer test := 40 + 50 - 40 - -10;

    ?. test & "";

    ?. "Inserisci un numero:";
    % number;

    ?. "Il valore di fibonacci per il numero " & number & " è uguale a " & fibonacci(number);

end main;